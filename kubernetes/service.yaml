apiVersion: v1
kind: Service
metadata:
  name:  bhedil-rtp-service
  namespace: bhedil
spec:
  type: NodePort
  selector:
    app: bhedil-rtp
  ports:
  - nodePort: 30082
    port: 8000
    targetPort: 8000